class Config {
  // Use 10.0.2.2 for Android Emulator, but here we use the specific host IP
  // to ensure it works on physical device if on same network.
  static const String baseUrl = "http://10.177.163.159:3000"; 
}
